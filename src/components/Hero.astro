---
---

<section class="relative min-h-screen flex items-center justify-center px-6 py-20 overflow-hidden">
  <!-- Simple background -->
  <div class="absolute inset-0 bg-gray-950"></div>

  <!-- Subtle purple glow -->
  <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-purple-600/5 rounded-full blur-3xl"></div>

  <div class="relative z-10 max-w-6xl mx-auto text-center">
    <!-- Logo -->
    <div class="mb-8 animate-fade-in-up opacity-0">
      <img src="/logo.png" alt="ClipConvo" class="h-24 md:h-32 mx-auto" />
    </div>

    <!-- Headline with clip-joining animation -->
    <h1 class="text-5xl md:text-7xl font-bold mb-6 flex flex-col items-center gap-2">
      <div class="flex items-center gap-12">
        <!-- Left clip -->
        <span class="text-white animate-[slideInLeft_0.6s_ease-out_forwards,slideTogetherLeft_0.3s_ease-out_1.3s_forwards] opacity-0">
          Edit Videos
        </span>

        <!-- Join separator <|> -->
        <span class="text-3xl md:text-5xl text-gray-500 font-mono animate-[fadeIn_0.3s_ease-out_0.6s_forwards,foldSeparator_0.4s_ease-out_0.9s_forwards] opacity-0">
          &lt;|&gt;
        </span>

        <!-- Right clip -->
        <span class="text-white animate-[slideInRight_0.6s_ease-out_forwards,slideTogetherRight_0.3s_ease-out_1.3s_forwards] opacity-0">
          Like Magic
        </span>
      </div>
    </h1>

    <!-- Subheadline -->
    <p class="text-xl md:text-2xl text-gray-300 mb-4 max-w-3xl mx-auto animate-fade-in-up delay-200 opacity-0">
      <span class="font-mono text-purple-400">"Trim the first 10 seconds"</span>
    </p>
    <p class="text-lg md:text-xl text-gray-400 mb-12 max-w-2xl mx-auto animate-fade-in-up delay-300 opacity-0">
      That's it. No timelines. No complex tools. Just you and your creativity.
    </p>

    <!-- CTA Button -->
    <a
      href="#beta-signup"
      class="inline-block bg-purple-600 hover:bg-purple-700 text-white font-semibold px-10 py-4 rounded-full text-lg transition-all transform hover:scale-105 shadow-lg shadow-purple-600/30 animate-fade-in-up delay-400 opacity-0"
    >
      Join Beta Waitlist →
    </a>

    <!-- Trust signal -->
    <p class="mt-6 text-sm text-gray-500 animate-fade-in-up delay-500 opacity-0">
      iOS · Android · macOS
    </p>

    <!-- Hero visual placeholder with better styling -->
    <div class="mt-20 animate-fade-in-up delay-600 opacity-0">
      <div class="relative rounded-2xl overflow-hidden shadow-2xl border border-gray-800 bg-gray-900">
        <div class="aspect-video bg-gray-900 flex items-center justify-center relative group">
          <!-- Center content -->
          <div class="relative z-10 text-center">
            <div class="w-16 h-16 mx-auto mb-4 rounded-2xl bg-purple-600 flex items-center justify-center">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z"/>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
            </div>
            <p class="text-gray-400">Demo video coming soon</p>
            <p class="text-sm text-gray-600 mt-2">Watch ClipConvo in action</p>
          </div>

          <!-- Shine effect on hover -->
          <div class="absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-500 bg-gradient-to-r from-transparent via-white/5 to-transparent translate-x-[-100%] group-hover:translate-x-[100%] transition-transform duration-1000"></div>
        </div>
      </div>
    </div>
  </div>
</section>
